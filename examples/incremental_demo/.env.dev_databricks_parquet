# Databricks Spark (parquet) defaults for the incremental demo
FF_SPARK_MASTER=local[*]
FF_SPARK_APP_NAME=incremental_demo_parquet

# Managed Hive-compatible metastore/database for parquet tables
FF_DBR_ENABLE_HIVE=1
FF_DBR_DATABASE=incremental_demo_parquet
# Optional: Unity Catalog overrides
# FF_DBR_CATALOG=hive_metastore

# Configure Java for local Spark sessions when needed
# JAVA_HOME=/opt/homebrew/opt/openjdk@17

# Leave FF_DBR_TABLE_FORMAT unset (defaults to parquet unless overridden)

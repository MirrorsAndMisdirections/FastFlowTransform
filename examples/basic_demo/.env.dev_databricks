# Databricks (or local Spark) profile defaults for the basic demo
FF_SPARK_MASTER=local[*]
FF_SPARK_APP_NAME=basic_demo

# Optional overrides when using Databricks SQL warehouses or Unity Catalog
# FF_DBR_DATABASE=basic_demo
# FF_DBR_CATALOG=hive_metastore

# Set these if you need a Hive-compatible Spark metastore
FF_DBR_ENABLE_HIVE=1

# Configure Java for local Spark sessions when needed
# JAVA_HOME=/opt/homebrew/opt/openjdk@17

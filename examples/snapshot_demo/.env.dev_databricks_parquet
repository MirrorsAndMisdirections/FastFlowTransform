# Databricks Spark (parquet) profile defaults for the snapshot demo
FF_SPARK_MASTER=local[*]
FF_SPARK_APP_NAME=snapshot_demo_parquet

# Managed metastore/database for parquet tables
FF_DBR_ENABLE_HIVE=1
FF_DBR_DATABASE=snapshot_demo_parquet
# Optional: Unity Catalog overrides
# FF_DBR_CATALOG=hive_metastore

# Configure Java for local Spark sessions when needed
# JAVA_HOME=/opt/homebrew/opt/openjdk@17

# Leave FF_DBR_TABLE_FORMAT unset (defaults to parquet unless Makefile overrides)
